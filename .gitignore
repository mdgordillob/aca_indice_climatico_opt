# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments / packaged env archives
myenv/
acaopt_env/
acaopt_env.zip
acaopt_env.7z

# Local secrets / machine-specific config — never commit real values here
src/scripts/keys.py
src/config.py
.cdsapirc

# Downloaded/generated data — large and regenerable from src/scripts
data/raw/era5/*
!data/raw/era5/.gitkeep
data/raw/processed/*
!data/raw/processed/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/indices/*
!data/indices/.gitkeep
data/zones/*
!data/zones/.gitkeep
*.nc

# LaTeX build artifacts (keep the .tex sources and compiled .pdf deliverables)
*.aux
*.bbl
*.bcf
*.blg
*.log
*.out
*.toc
*.run.xml

# IDE metadata
.vs/

# Excel temp files
*.~*
